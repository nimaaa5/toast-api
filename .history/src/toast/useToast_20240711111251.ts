import {useState, useEffect} from "react";


const useToast = () => {

  const addToast = (message:string)=>{
    const [showToast, showToast] = useState<boolean>(false);
    useEffect(() => {
      setInterval()
    })
  }

  return {addToast};
}

