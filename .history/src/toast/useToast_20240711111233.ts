import {useState, useEffect} from "react";


const useToast = () => {

  const addToast = (message:string)=>{
    const [showToast, showToast] = setState(false);
    useEffect(() => {
      setInterval()
    })
  }

  return {addToast};
}

